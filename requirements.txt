streamlit==1.27.0
pdfplumber==0.8.0
pandas==2.2.2
openpyxl==3.1.2
matplotlib==3.7.2
python-dotenv==1.0.0

# Google Gemini API
google-generative-ai==0.1.0

# LangChain components (matching the import paths used)
langchain==0.0.297
langchain-community==0.0.21

# Embeddings (fallback vector store + HuggingFace)
sentence-transformers==2.2.2

# FAISS — optional; Streamlit Cloud MAY FAIL — but safe with fallback in app.py
faiss-cpu==1.7.4
